# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/patent_intelligence

# Redis
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# USPTO API (free, no key required for basic access)
USPTO_API_KEY=

# EPO OPS API (register at https://developers.epo.org/)
EPO_CONSUMER_KEY=
EPO_CONSUMER_SECRET=

# AI/LLM Keys (optional, for idea generation features)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Embedding model
EMBEDDING_MODEL=AI-Growth-Lab/PatentSBERTa

# Auth (REQUIRED in production - generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# In debug mode only, auto-generated if not set (sessions won't persist across restarts)
SECRET_KEY=

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Debug
DEBUG=false
